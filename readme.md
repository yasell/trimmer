### Шаблон встроенного лендинга

- шаблон для лендинга карточки товара внутри сайта
- for `gardentool.by`

Установка:
- `npm install`

Юзкейс:
- `npm start`
- вжухх и браузер запущен, изменения в файлах отслеживаются...

Итоговая сборка проекта:
- `npm run build` (сгенерит папочку `build` в корне)
